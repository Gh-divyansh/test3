import React , {Component} from "react";

class welcome extends Component {
    render() {
        return <h1> class Component {this.props.name} , {this.props.hero}</h1>
    }
}

export default welcome;